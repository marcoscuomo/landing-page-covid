import styled from "styled-components";

export const Container = styled.div`
  h1 {
    color: var(--orange-200);
    font-size: 1.3rem;
  }

  h2 {
    color: var(--green-300);
    font-size: 2.2rem;
    margin-top: 10px;
  }
`;